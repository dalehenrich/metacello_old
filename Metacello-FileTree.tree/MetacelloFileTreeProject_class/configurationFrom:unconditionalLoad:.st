
!MetacelloFileTreeProject class methodsFor: 'scripting'!
configurationFrom: aScriptingEngine unconditionalLoad: unconditionalLoad
    "for now we don't have a configuration class as an intermediate result...we just create the project directly"

    self shouldNotImplement! !
